export interface TourDetail {
  contentid: string;
  title: string;
  homepage?: string;
  overview: string;
  tel: string;
  telName: string;
  firstImage?: string;
  addr1: string;
  restDate?: string;  // 휴무일
}
